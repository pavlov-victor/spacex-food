# API-проверка product — 2026-09-12

## Повторный тест 0.0.2 с фото ресторана

- Опубликованная версия 0.0.2 подтверждена через `/info`. Фото `resources/c-1562004473-3635391617.jpg` загружено через Dify `/files/upload`; полученный подписанный `source_url` передан в `table_image_url`.
- Run `74e0dd01-25e0-4585-a50a-53a1223527e0`: 9.87 секунды, Dify `partial-succeeded`, выход `status: partial`. Текст, переводы, классификация и валидация успешны. Оба HTTP-узла Exa и Image возвращают `Unknown error`; причина внутри Dify пока не установлена.
- Точный запрос `/images/edits`, подготовленный Code-узлом Dify, отправлен напрямую в x.ai с локальным ключом. Успешно получено изображение блюда на столе в предоставленном интерьере; визуально проверено сохранение узнаваемого помещения и клетчатой скатерти.
- Картинка: `tmp/product-on-table.jpg`; API-события: `tmp/product-table-run.json`. `/tmp/` добавлен в `.gitignore` и проверен через `git check-ignore`. Изображение получено прямым x.ai вызовом, а не HTTP-узлом Dify. Рецептура и подача остаются неподтверждённой AI-иллюстрацией.
- 10 локальных тестов повторно прошли. Для полного успешного запуска остаётся исправить конфигурацию HTTP-узлов в Dify; проверить секреты Environment Variables и подробную ошибку в редакторе.

## Первый тест 0.0.1

Ключ `DIFY_PERSON_API` успешно авторизует приложение `spacex-product`: `GET /v1/info` и `GET /v1/parameters` вернули 200. Проверенные входы совпадают с локальным `example-inputs.json`.

Два запуска опубликованной версии 0.0.1 через `POST /v1/workflows/run`:

- Вход и подготовка данных успешны.
- Exa HTTP-узел падает с `Unknown error` примерно за 0.0003 с; fallback работает. Точная причина недоступна через полученные события API. Проверить секрет `EXA_API_KEY` в Environment Variables и настройки HTTP-узла. Это не подтверждённая ошибка ключа: прямой запрос Exa с локальным ключом успешен (200, источник с текстом).
- Grok успешно вернул описание, переводы sr/en/ru, классификацию и промпт изображения.
- Узел проверки упал на `vegan.value: "false"`: модель выдала строку вместо boolean. Логи: `20260912-124524-product.json`, `diagnostic-20260912-124552.json`.
- До Image в опубликованной Dify-версии выполнение не дошло. Полный Dify workflow пока НЕ прошёл проверку.

Исправление подготовлено в `../0.0.2.yml`: строки `true`/`false` нормализуются только в AI-предположениях, подтверждения ресторана остаются строгими. Промпт уточнён. Реальный сохранённый ответ Grok проходит новый валидатор; 10 локальных тестов прошли. Версия 0.0.1 не изменялась, новая версия пока не опубликована.

Прямой тест x.ai Image выполнен на промпте реального ответа Grok после исправления валидатора. Модель `grok-imagine-image-2.0` вернула URL; изображение скачано в `karadjordjeva-generated.jpg`, полный собранный объект — `direct-image-test.json`. Этот объект имеет статус `partial`, так как исходный ответ Grok получен без работающего поиска; это НЕ успешный запуск всего нового графа в Dify.

Визуальная проверка: изображение открывается, показывает панированный рулет на тарелке. Модель добавила начинку, соус и гарнир, которых ресторан не подтверждал; результат пригоден как черновая иллюстрация, а не подтверждённая фотография блюда. Режим с референсом стола в этом тесте не запускался.

Следующий шаг: импортировать/опубликовать product 0.0.2, заполнить `EXA_API_KEY` и `XAI_API_KEY` в Dify Environment Variables, затем повторить API-тест. `DIFY_PERSON_API` — ключ запуска приложения; доступа к редактору/импорту он не предоставляет.
