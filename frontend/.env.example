# Public URL, not a credential. `npx convex dev` writes the real value to .env.local.
VITE_CONVEX_URL=https://your-deployment.convex.cloud
# Never expose Dify/x.ai/Exa API keys, JWT_PRIVATE_KEY or CONVEX_DEPLOY_KEY as VITE_ variables.
